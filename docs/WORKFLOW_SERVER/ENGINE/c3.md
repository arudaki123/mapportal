---
sidebar_position: 6
---

# VBD WF Engine Gateway
Những script task được viết bằng js script 
output của activity gateway là true,false sẽ chạy qua flow có name tương ứng. 
![Sequence](../Image\gateway\g1.png)
# ví dụ cơ bản tạo 1 random number có chia hết cho 2 hay không. 
activity **create random number** add script 
```js title="/js/create random number"
function getRandomInt(max) {
  return Math.floor(Math.random() * max);
}
 console.log(activity.name + ' run'); 
let num = getRandomInt(10); 
console.log('create random number' + ' ' + num); //create random 1-3 
activity.setOutput(num); //set output cho activity 
```
activit **gate way** add start event script 
![Sequence](../Image\gateway\g2.png)
```js title="/js/gateway"
let random_number = activity.getInput(); //get random number
let b = random_number % 2 === 0; // kiem tra co chia het cho 2 hay khong
activity.setOutput(b); //set output
```
tạo 2 flow true, and false **phải đặt tên flow là true hoặc false thì activity gateway mới chạy được** 
thêm script vào 2 activity **divisble by 2** và **not divisible by 2**

**divisble by 2**
![Sequence](../Image\gateway\g3.png)

**not divisible by 2**
![Sequence](../Image\gateway\g4.png)
run wf 
```cmd
create random number run
create random number 9
not divisible by 2
```